# 牛（中立生物）功能实现文档

## 功能概述
添加了一种新的中立生物"牛"，具有独特的行为模式，与敌对生物完全不同。

## 生物特性

### 基础属性
- **生命值（HP）**: 60
- **移动速度（Speed）**: 50（比普通敌人慢）
- **攻击力（Attack）**: 0（不会攻击玩家）
- **索敌圈（Aggro）**: 0（没有索敌圈）
- **警戒圈（Alert）**: 0（没有警戒圈）
- **碰撞半径（Touch Radius）**: 16
- **墙壁碰撞半径**: 8
- **护盾**: 无（shieldMax: 0, armor: 0）
- **颜色**: #8b7355（棕色）

### 行为特性
1. **中立性**
   - 不会主动攻击玩家
   - 不会因为玩家靠近而进入警戒状态
   - 攻击力为0，即使碰到玩家也不造成伤害

2. **状态系统**
   - **休息状态（Rest）**: 默认状态，缓慢随机游荡
   - **巡逻状态（Patrol）**: 被攻击后进入此状态，加速移动
   - **状态转换**:
     - 被攻击 → 立即进入Patrol状态
     - Patrol状态持续10秒无攻击 → 回到Rest状态
     - 不会因为玩家靠近而改变状态

3. **视觉表现**
   - 使用两张图片素材实现跑动动画
     - 素材1：`素材/mow1.png`
     - 素材2：`素材/mow2.png`
   - 图片大小为碰撞半径的2倍（32x32像素）
   - 在小地图上显示为棕色点
   - **方向感**：根据移动方向自动翻转
     - 向左移动：模型正常显示
     - 向右移动：模型横向翻转
     - 静止时：保持上次移动方向
   - **跑动动画**：两帧交替切换
     - 切换频率：每200ms（5帧/秒）
     - 触发条件：patrol状态或attack状态
     - rest状态：显示第一帧，无动画
   - **移动抖动**：配合跑动动画的上下抖动
     - 抖动幅度：2像素
     - 抖动频率：周期性sin波动
     - 触发条件：patrol状态或attack状态

## 技术实现

### 1. 类型定义（第2616行）
```javascript
cow: { 
  hp: 60, 
  spd: 50, 
  atk: 0, 
  aggro: 0, 
  touchR: 16, 
  color: '#8b7355', 
  wallCollideR: 8, 
  armor: 0, 
  shieldMax: 0 
}
```

### 2. 图片加载（第24-31行）- 两帧动画
```javascript
// cow sprites (two frames for running animation)
const cowImg1 = new Image(); 
let cowImg1Ready = false;
const cowImg2 = new Image(); 
let cowImg2Ready = false;
cowImg1.onload = () => { cowImg1Ready = true; };
cowImg2.onload = () => { cowImg2Ready = true; };
cowImg1.src = '素材/mow1.png';
cowImg2.src = '素材/mow2.png';
const cowImgReady = () => cowImg1Ready && cowImg2Ready; // 两张都加载完成才算ready
```

### 3. AI逻辑修改（第3717-3724行）
```javascript
// 牛不会因为玩家靠近而进入patrol，只有被攻击才会
if(e.type !== 'cow' && dp < alertR){ 
  e.state='patrol'; 
  e.patrolSince = performance.now(); 
}
// 牛在patrol状态持续长一些（10秒），其他敌人5秒
const patrolDuration = (e.type === 'cow') ? 10000 : 5000;
if(!e.attack && since > patrolDuration){ 
  e.state='rest'; 
  e.restHoursForShield = 0; 
  e.lastRestHourStamp = totalHours; 
}
```

### 4. 渲染逻辑（第3982-4025行）- 方向翻转 + 跑动动画 + 移动抖动
```javascript
// 牛使用图片，其他敌人使用圆形
if(e.type === 'cow' && cowImgReady()){
  // 使用图片原始宽高比例，不强制拉伸为正方形
  const cowScale = 2.0; // 牛的缩放系数，调整显示大小（缩小到80%）
  const baseSize = e.r * 2 * cowScale; // 基准尺寸（增加缩放）
  const imgWidth = cowImg1.naturalWidth || cowImg1.width;
  const imgHeight = cowImg1.naturalHeight || cowImg1.height;
  const aspectRatio = imgWidth / imgHeight;
  
  // 按高度为基准缩放，保持宽高比
  const drawHeight = baseSize;
  const drawWidth = drawHeight * aspectRatio;
  
  ctx.save();
  
  // 根据速度判断朝向
  if(e.vx > 0.1) e.facingRight = true;
  else if(e.vx < -0.1) e.facingRight = false;
  // 如果速度接近0，保持上次朝向（不改变facingRight）
  
  // 移动时的上下抖动效果（只在patrol或attack状态时）
  let bounceOffset = 0;
  if(e.state === 'patrol' || e.attack){ // 只在巡逻或攻击时抖动
    const time = performance.now() * 0.01; // 控制抖动速度
    bounceOffset = Math.sin(time) * 4; // 抖动幅度为4像素（增大一倍）
  }
  
  // 跑动动画：在patrol或attack状态时在两张图片之间切换
  let currentFrame;
  if(e.state === 'patrol' || e.attack){
    // 每200ms切换一次帧（5帧/秒）
    const frameTime = Math.floor(performance.now() / 200) % 2;
    currentFrame = frameTime === 0 ? cowImg1 : cowImg2;
  } else {
    // rest状态使用第一帧
    currentFrame = cowImg1;
  }
  
  // 如果朝右，翻转图片
  if(e.facingRight){
    ctx.translate(ex, ey + bounceOffset);
    ctx.scale(-1, 1);
    ctx.drawImage(currentFrame, -drawWidth/2, -drawHeight/2, drawWidth, drawHeight);
  } else {
    ctx.drawImage(currentFrame, ex - drawWidth/2, ey - drawHeight/2 + bounceOffset, drawWidth, drawHeight);
  }
  
  ctx.restore();
} else {
  const c = e.color || '#c33';
  ctx.fillStyle = c; 
  ctx.beginPath(); 
  ctx.arc(ex, ey, e.r, 0, Math.PI*2); 
  ctx.fill();
}
```

**宽高比保持原理**：
- 统一使用第一张图片（cowImg1）的尺寸作为基准
- 读取图片的原始尺寸：`naturalWidth`和`naturalHeight`
- 计算宽高比：`aspectRatio = imgWidth / imgHeight`
- 使用缩放系数：`cowScale = 2.0`（可调整显示大小）
- 计算基准尺寸：`baseSize = e.r * 2 * cowScale`
- 以高度为基准：`drawHeight = baseSize`
- 自动计算宽度：`drawWidth = drawHeight * aspectRatio`
- 无论切换到哪一帧，都使用相同的drawWidth和drawHeight
- 确保两帧显示大小完全一致，即使素材分辨率不同
- 避免强制正方形导致的拉伸变形
- 完全保持素材的原始比例
- 通过调整 `cowScale` 可轻松改变牛的显示大小

**方向翻转原理**：
- 检测牛的水平速度`vx`来判断移动方向
- `vx > 0.1`：向右移动，设置`facingRight = true`，翻转图片
- `vx < -0.1`：向左移动，设置`facingRight = false`，正常显示
- 速度接近0时保持上次的朝向，避免静止时频繁闪烁
- 使用canvas的`translate`和`scale(-1, 1)`实现水平翻转

**跑动动画原理**：
- 使用两张不同的图片表示牛跑动的不同姿态
- 检查牛的状态：`e.state === 'patrol' || e.attack`
- 仅在patrol（巡逻）或attack（攻击）状态时切换帧
- rest（休息）状态时固定显示第一帧
- 使用时间戳决定当前帧：`Math.floor(performance.now() / 200) % 2`
- 每200ms切换一次（5帧/秒），产生流畅的跑动效果
- 两帧交替切换：`frameTime === 0 ? cowImg1 : cowImg2`

**移动抖动原理**：
- 检查牛的状态：`e.state === 'patrol' || e.attack`
- 仅在patrol（巡逻）或attack（攻击）状态时启用抖动
- rest（休息）状态时完全无抖动
- 使用`performance.now() * 0.01`作为时间基准
- 通过`Math.sin(time)`生成周期性的上下偏移
- 偏移幅度为±4像素，让牛看起来像在"跳跃"移动
- 基于状态判断比速度判断更准确，避免rest状态的缓慢游荡也触发抖动
- 与帧动画配合，增强跑动的真实感

### 5. 生成逻辑（第2677-2701行）
```javascript
function spawnCows(n){
  const { rng } = world;
  for(let i=0;i<n;i++){
    const ex = (rng.int(3000)-1500);
    const ey = (rng.int(3000)-1500);
    const e = makeEnemy('cow');
    let pos = findLandNear(ex, ey, 80, 240, 24);
    // ... 位置检测和碰撞检测 ...
    // 确保牛不会离玩家太近（最小300像素）
    if(dp0 < 300){
      const altFar = findLandNear(player.x, player.y, 400, 800, 24);
      if(altFar) pos = altFar; else { continue; }
    }
    e.x = pos.x; e.y = pos.y; enemies.push(e);
  }
}
// 游戏初始化时生成8-12只牛
spawnCows(8 + Math.floor(Math.random() * 5));
```

## 游戏体验

### 初始分布
- 游戏开始时在世界各地生成8-12只牛
- 分布范围：玩家周围3000x3000像素区域
- 最小距离：距离玩家至少300像素

### 互动方式
1. **攻击牛**: 玩家可以使用任何武器攻击牛
2. **牛的反应**: 被攻击后进入巡逻状态，移动速度提升
3. **冷却机制**: 10秒不被攻击后牛会冷静下来，回到休息状态
4. **掉落物**: 牛死亡后不会掉落特殊物品（使用默认掉落系统）

### 调试信息
- 按G键可显示生物的索敌圈和警戒圈
- 牛的索敌圈为0，不会显示任何警戒范围
- 可在小地图上观察牛的位置（棕色点）

## 文件修改清单

| 文件 | 修改内容 |
|------|---------|
| `src/sandbox.js` | 添加cow类型、图片加载、AI逻辑、渲染逻辑、生成逻辑 |
| `素材/mow.png` | 牛的图片素材（外部文件） |

## 扩展建议

### 未来可能的改进
1. **掉落系统**: 为牛添加特殊掉落物（如牛奶、皮革等）
2. **繁殖系统**: 让牛可以繁殖，增加数量
3. **喂养系统**: 玩家可以喂养牛，提高友好度
4. **驯化系统**: 驯化后的牛可以跟随玩家或提供资源
5. **声音效果**: 添加牛的叫声音效
6. **多样化**: 添加不同品种的牛（如奶牛、公牛等）

## 测试建议

1. **基础测试**
   - 游戏启动后检查是否生成了牛
   - 靠近牛观察是否保持休息状态
   - 攻击牛观察是否进入巡逻状态

2. **行为测试**
   - 攻击后等待10秒，观察是否回到休息状态
   - 连续攻击牛，观察巡逻状态是否持续
   - 检查牛是否会攻击玩家（应该不会）

3. **视觉测试**
   - 确认牛显示为图片而非圆形
   - 检查小地图上牛的颜色是否为棕色
   - 按G键检查牛没有显示索敌圈

4. **方向翻转测试**
   - 观察牛向左移动时图片朝向
   - 观察牛向右移动时图片是否翻转
   - 检查牛静止时是否保持上次朝向
   - 确认转向过程平滑，无闪烁

5. **移动抖动测试**
   - 观察牛移动时是否有上下抖动
   - 检查静止时抖动是否停止
   - 确认抖动幅度适中，不影响视觉
   - 验证抖动频率连贯平滑

6. **跑动动画测试**
   - 观察牛在patrol状态时是否在两帧之间切换
   - 检查rest状态时是否显示静止帧
   - 确认帧切换频率流畅自然（每200ms）
   - 验证所有牛的动画不是完全同步的（基于全局时间）
   - 检查两张图片是否都能正确加载显示

## 注意事项

1. **图片路径**: 确保`素材/mow1.png`和`素材/mow2.png`两个文件都存在且路径正确
2. **图片加载**: 两张图片必须都加载完成才会显示牛，否则显示为圆形
3. **图片比例**: 自动使用图片原始宽高比，不会拉伸变形，完全保持素材原貌
4. **尺寸统一**: 统一使用第一张图片（mow1.png）的尺寸作为基准，确保两帧大小一致
5. **显示大小**: 通过 `cowScale` 参数控制（当前为2.0），可根据素材分辨率调整
6. **抖动幅度**: 通过 `bounceOffset` 控制（当前为4像素），让移动更加生动
7. **性能影响**: 初始生成8-12只牛，使用两帧动画和抖动效果，性能影响仍然较小
8. **兼容性**: 牛使用现有的敌人系统框架，与其他敌人类型兼容
9. **状态持久化**: 牛的状态不会在重新加载后保存
10. **方向判断阈值**: 使用±0.1的速度阈值避免静止时频繁翻转
11. **抖动触发条件**: 基于状态判断（patrol/attack），确保只在激活移动时才抖动
12. **动画流畅度**: 帧动画和抖动动画都独立于游戏帧率，使用`performance.now()`确保流畅
13. **状态系统**: rest状态时牛会缓慢游荡但不抖动也不切换帧，只有被攻击进入patrol状态才会有完整动画
14. **动画同步**: 所有牛使用相同的全局时间戳，因此同一时刻处于相同状态的牛会显示相同的帧

## 更新历史

### v1.3.4 - 2025年11月5日 10:01
- 🔧 **调整**: 优化模型大小和抖动效果
  - 缩小牛的模型到80%（cowScale从2.5改为2.0）
  - 增大向上抖动幅度到2倍（从2像素改为4像素）
  - 让牛的移动更加生动活泼

### v1.3.3 - 2025年11月5日 09:54
- 🐛 **修复**: 统一两帧显示大小
  - 修复两张图片尺寸不同导致切换时大小变化的问题
  - 统一使用第一张图片（mow1.png）的尺寸作为基准
  - 无论切换到哪一帧，都使用相同的drawWidth和drawHeight
  - 确保动画播放时牛的大小始终一致

### v1.3.2 - 2025年11月5日 09:44
- 🔧 **调整**: 增加牛的显示尺寸
  - 添加缩放系数 `cowScale = 2.5`
  - 修复新素材导致的牛显示过小问题
  - 现在牛的显示尺寸恢复到合适大小
  - 可通过调整 `cowScale` 参数轻松控制显示大小

### v1.3.1 - 2025年11月5日 09:42
- 🐛 **修复**: 图片拉伸变形问题
  - 修复牛的模型被上下拉伸的问题
  - 现在使用图片原始宽高比例绘制
  - 按高度为基准缩放，自动计算宽度
  - 完全保持素材的原始比例，不再强制正方形

### v1.3 - 2025年11月5日 09:39
- ✨ **新增**: 两帧跑动动画
  - 使用两张图片（mow1.png和mow2.png）实现跑动动画
  - 每200ms切换一次帧，产生流畅的跑步效果
  - 只在patrol或attack状态时播放动画
  - rest状态显示静止的第一帧
  - 动画与上下抖动配合，增强真实感
  - 所有牛的动画基于全局时间，同步显示

### v1.2.1 - 2025年11月5日 09:34
- 🐛 **修复**: 抖动触发条件bug
  - 修复牛在rest状态时也会抖动的问题
  - 改为基于状态判断而非速度判断
  - 现在只在patrol或attack状态时抖动
  - rest状态完全静止，无抖动

### v1.2 - 2025年11月5日 09:29
- ✨ **新增**: 移动抖动动画
  - 移动时模型微微向上抖动
  - 抖动幅度2像素，自然流畅
  - 静止时无抖动
  - 基于sin函数的周期性动画
  - 使牛更加生动活泼

### v1.1 - 2025年11月5日 09:22
- ✨ **新增**: 方向翻转功能
  - 向左移动时模型正常显示
  - 向右移动时模型横向翻转
  - 静止时保持上次移动方向
  - 添加速度阈值避免闪烁

### v1.0 - 2025年11月5日 09:15
- 🎉 初始版本
  - 基础属性和行为
  - AI逻辑实现
  - 图片素材集成
  - 生成系统

## 作者
Windsurf Cascade AI Assistant
